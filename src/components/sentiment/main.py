import pandas as pd

def loadData(path):
    # TODO(aaronhma): Load the data
    pass

def cleanData(data) -> pd.DataFrame():
    # TODO(aaronhma): Clean the data
    return pd.DataFrame({"test0": 0, "test1": 1})
